export interface Cuisine {
  id: string;
  name: string;
  description: string;
  image?: string;
}

export interface MenuItem {
  id: string;
  name: string;
  description: string;
  image?: string;
  price?: string;
}
